# 💬 댓글 기능 켜기 (한 번만 하면 됩니다)

댓글을 저장할 **무료 창고(Firebase)**를 만들고 블로그에 연결하는 방법이에요.
조금 길어 보이지만, 천천히 따라 하면 15분이면 끝나요. 무료이고 카드 등록도 필요 없어요.

> 준비물: 구글 계정 (지메일 있으면 OK)

---

## 1단계. Firebase 프로젝트 만들기

1. 인터넷에서 **https://console.firebase.google.com** 에 접속해 구글 계정으로 로그인
2. **"프로젝트 만들기"** (Create a project) 클릭
3. 프로젝트 이름을 아무거나 입력 (예: `my-blog`) → 계속
4. "Google 애널리틱스"는 **꺼도(사용 안 함) 됩니다** → 계속 / 만들기
5. 잠시 기다리면 프로젝트가 만들어져요

---

## 2단계. 댓글 저장소(Firestore) 켜기

1. 왼쪽 메뉴에서 **"빌드(Build) → Firestore Database"** 클릭
2. **"데이터베이스 만들기"** (Create database) 클릭
3. 위치는 그냥 기본값(또는 `asia-northeast3 (서울)`)으로 두고 다음
4. 보안 규칙은 **"테스트 모드에서 시작"**을 골라도 되고, 무엇을 골라도 괜찮아요.
   (어차피 4단계에서 우리가 직접 바꿀 거예요) → 만들기

---

## 3단계. 웹 앱 등록하고 '연결 정보' 받기

1. 왼쪽 위 **⚙️(톱니바퀴) → "프로젝트 설정"** 클릭
2. 아래로 내려서 **"내 앱"** 영역의 **`</>` (웹) 아이콘** 클릭
3. 앱 닉네임 아무거나 입력 (예: `blog`) → **"앱 등록"**
   (호스팅 체크박스는 안 해도 돼요)
4. 그러면 아래처럼 생긴 **연결 정보**가 나와요. 이게 핵심이에요!

   ```js
   const firebaseConfig = {
     apiKey: "AIzaSyB.......",
     authDomain: "my-blog.firebaseapp.com",
     projectId: "my-blog",
     storageBucket: "my-blog.appspot.com",
     messagingSenderId: "1234567890",
     appId: "1:1234:web:abcd..."
   };
   ```

   > 💡 이 값들은 인터넷에 공개돼도 안전하도록 설계돼 있어요. (진짜 보안은 4단계 규칙이 담당해요)

---

## 4단계. 연결 정보를 블로그에 붙여넣기

1. 우리 블로그 폴더의 **`assets/firebase-config.js`** 파일을 엽니다.
2. 거기 있는 예시값을, 방금 3단계에서 받은 **내 값으로** 바꿔 넣습니다.
   따옴표(`"`)는 그대로 두고 **안쪽 글자만** 바꾸면 돼요.

   바꾸기 전:
   ```js
   apiKey: "여기에-내-apiKey-붙여넣기",
   ```
   바꾼 후 (예시):
   ```js
   apiKey: "AIzaSyB.......",
   ```

   6개 항목(apiKey, authDomain, projectId, storageBucket,
   messagingSenderId, appId)을 모두 내 값으로 채워주세요.

---

## 5단계. 댓글 보안 규칙 설정 (스팸/장난 방지)

누구나 댓글은 쓸 수 있되, 남의 댓글을 함부로 지우거나 고치지 못하게 막는 단계예요.

1. Firebase 콘솔에서 **"Firestore Database" → 위쪽 "규칙(Rules)" 탭** 클릭
2. 내용을 모두 지우고 아래를 그대로 붙여넣은 뒤 **"게시(Publish)"**

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /comments/{id} {
      allow read: if true;
      allow create: if request.resource.data.name is string
                    && request.resource.data.name.size() > 0
                    && request.resource.data.name.size() < 40
                    && request.resource.data.text is string
                    && request.resource.data.text.size() > 0
                    && request.resource.data.text.size() < 1000
                    && request.resource.data.postId is string;
      allow update, delete: if false;
    }
  }
}
```

이렇게 하면:
- ✅ 누구나 댓글을 **읽고 쓸 수** 있어요
- ✅ 너무 긴 글이나 빈 글은 막혀요
- ✅ 한번 쓴 댓글을 남이 **고치거나 지울 수 없어요**

> 🧹 내가 직접 댓글을 지우고 싶을 땐? Firebase 콘솔의 Firestore 화면에서
> `comments` 항목을 열어 마음에 안 드는 댓글을 직접 삭제할 수 있어요.

---

## 6단계. 저장하고 올리기

`firebase-config.js` 파일을 저장한 뒤 GitHub에 올리면(커밋·푸시),
1~2분 뒤 블로그 글 아래에 **댓글창이 나타납니다.** 🎉

테스트로 이름과 댓글을 직접 한번 남겨보세요. 바로 보이면 성공이에요!

---

## ❓ 잘 안 될 때

- 댓글창에 "준비 중"이라고 떠요 → `firebase-config.js`에 값이 아직 예시 그대로예요. 4단계 확인.
- "불러오지 못했어요"가 떠요 → 5단계 규칙을 게시했는지, 2단계에서 Firestore를 만들었는지 확인.
- 그래도 안 되면, 화면을 캡처해서 저(클로드)에게 보여주세요. 같이 봐드릴게요!
